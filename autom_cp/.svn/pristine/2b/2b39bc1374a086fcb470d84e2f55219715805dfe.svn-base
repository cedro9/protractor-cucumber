"use strict";

var { Given } = require("cucumber");

const { cadastre } = require("../pages/classificationsPage");

Given('que cadastro a classificação {string} do tipo {string}', function (classification, classificationType) {
    return cadastre(classification, classification, classification, classificationType);
});